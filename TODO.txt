# Remove headline # -*- coding: utf-8 -*-------------- recorder -------------    _recorder.py -> TODO    stream_recorder.py -> TO TEST-------------- player --------------    stream_player.py -> TO TEST        -> Confirm that while/pass works in .start()        -> Check if TRIGGER channel is streamed        -> Create post on SO to get a better syntax            tch = self.get_trg_index()            idx = [k for k in range(self.raw._data.shape[0]) if k != tch]            self.raw._data[idx, :] = self._raw.get_data()[idx, :] * 1E6------------- triggers -------------triggers.trigger_def -- TO CHECKtriggers.trigger -- TO CHECK---------- utils ----------utils.benchmark -> TODOutils.events    .brainvision.fix_default_event_values -- DONE    .change_event_values -- DONE    .find_event_channel -- DONE    .merge_events -> TODOutils.io    .convert2fif -> TODO & rename to read_raw.py    .export -> TO TEST    .io_file_dir -- DONE    .load_config -- DONE    .load_mat -- DONE    .read_raw_fif -- DONEutils.layouts    .caps -- DONEutils.lsl    .lsl -- DONEutils.math    .math -- DONEutis.preprocess    .preproces -> TODO -> To remove    .mne.rename_channels -- DONE    .mne.set_channel_types -- DONE    .mne.set_eeg_reference -- DONE    .mne.resample -- DONE    .mne.filter -> TODO    .np.reorder_channels -- DONE    .np.set_eeg_reference -- DONE    .np.filter -> TODOutils.timer    .timer -- DONE------Check all __init__.py from utils.# Set headline #!/usr/bin/env python3 for every executable scripts